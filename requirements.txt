# ==============================================================================
# DreamCatalyst-NS — Python requirements
# ==============================================================================
# Install order (handled by setup.sh):
#   1. PyTorch 2.1.2+cu118
#   2. pip install -e ./nerfstudio   (cloned from upstream)
#   3. pip install -e ./threestudio  (cloned from upstream)
#   4. pip install -r requirements.txt
#   5. pip install -e .
# ==============================================================================

# ── Gaussian Splatting backend ───────────────────────────────────────────────
gsplat==1.4.0

# ── Diffusion / SDS guidance ────────────────────────────────────────────────
# Pinned upper bounds: newer versions require torch.xpu (PyTorch ≥2.4) or numpy≥2
diffusers>=0.27.0,<0.31.0
transformers>=4.36.0,<4.46.0
accelerate>=0.25.0,<0.34.0

# ── Numerical / utility ─────────────────────────────────────────────────────
numpy<2
opencv-python<4.11
Pillow
imageio
imageio-ffmpeg
open3d

# ── Logging / visualisation ─────────────────────────────────────────────────
tensorboard
